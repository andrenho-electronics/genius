#include "queue.h"

#include <stdio.h>

int main()
{
    /*
    queue_init();
    for (uint8_t i = 0; i < 32; ++i)
        queue_increase();
    for (uint8_t i = 0; i < queue_size(); ++i)
        printf("%d ", queue_item(i));
    printf("\n");
    return 0;
    */
}

// vim:st=4:sts=4:sw=4:expandtab
